require 'scanner'

class Merger
  
  def initialize
    @scanner = Scanner.new
  end

  def merge(source, destination)
    #@scanner.scan_folder(source) | @scanner.scan_folder(destination)
    # manual merge of two arrays of objects
    
    list = Array.new
    
    
    
    list
  end
  
end